{
  "name": "lermo-online",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install --prefix backend --omit=dev",
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": ">=18"
  }
}